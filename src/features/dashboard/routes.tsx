import React from 'react';
import { Dashboard } from './Dashboard';

// This file defines the routes for the dashboard feature module

// Define routes for the dashboard feature
export const dashboardRoutes = [
  {
    path: '/dashboard',
    element: <Dashboard />,
  }
];
